import { main } from "framer-motion/client";

export default function Register(){
    return(
        <main>
            <div className="flex items-center justify-center">
                <h1 className="text-[6rem]">We'll be live with the registration form soon</h1>
            </div>
        </main>

    )
}